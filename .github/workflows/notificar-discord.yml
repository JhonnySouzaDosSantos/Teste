name: Notificar no Discord

on:
  push:
    branches:
      - '**'

jobs:
  enviar-mensagem:
    runs-on: ubuntu-latest

    steps:
      - name: Checar o código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Enviar notificação para o Discord
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          GITHUB_EVENT_BEFORE: ${{ github.event.before }}
          GITHUB_SHA: ${{ github.sha }}
        run: |
          if [ "$GITHUB_EVENT_BEFORE" = "0000000000000000000000000000000000000000" ]; then
            COMMITS=$(git log -10 --pretty=format:"**🆔 Hash:** `%h`\n**👤 Usuario:** %an\n**💬 Mensagem:** %s\n\n")
          else
            COMMITS=$(git log --pretty=format:"**🆔 Hash:** `%h`\n**👤 Usuario:** %an\n**💬 Mensagem:** %s\n\n" $GITHUB_EVENT_BEFORE..$GITHUB_SHA)
          fi

          if [ -z "$COMMITS" ]; then
            COMMITS="Nenhum commit encontrado."
          fi

          COMMITS_ESCAPED=$(echo "$COMMITS" | python3 -c 'import json,sys; print(json.dumps(sys.stdin.read()))')

          # Cabeçalho fixo
          MESSAGE="📌 **Novo Push no Repositório!**"
          MESSAGE+="\n🌿 **Branch:** ${{ github.ref_name }}"
          MESSAGE+="\n🔢 **Total de Commits:** $(echo "$COMMITS" | wc -l)\n"

          # Processa cada commit
          while IFS='|' read -r HASH USER MSG; do
            MESSAGE+="\n――――――――――――――――――"
            MESSAGE+="\n👤 **Usuário:** $USER"
            MESSAGE+="\n🆔 **Hash:** \`$HASH\`"
            MESSAGE+="\n💬 **Mensagem:** $MSG"
          done <<< "$COMMITS"

          # Envia usando curl com tratamento de erro
          curl -H "Content-Type: application/json" \
               -X POST \
               -d "{\"content\": \"📌 **Novos Commits Recebidos:**\n\n$COMMITS_ESCAPED\n—————————————————————\"}" \
               $DISCORD_WEBHOOK_URL
