name: Notificar no Discord

on:
  push:
    branches:
      - '**'

jobs:
  enviar-mensagem:
    runs-on: ubuntu-latest

    steps:
      - name: Checar o código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # importante para pegar todo o histórico e múltiplos commits

      - name: Enviar notificação para o Discord
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          GITHUB_EVENT_BEFORE: ${{ github.event.before }}
          GITHUB_SHA: ${{ github.sha }}
        run: |
          # Pega todos os commits entre o commit antes do push e o atual
          COMMITS=$(git log --pretty=format:"**🆔 Hash:** `%h`\n**👤 Usuario:** %an\n**💬 Mensagem:** %s\n\n" $GITHUB_EVENT_BEFORE..$GITHUB_SHA)

          # Se não tiver commits, define uma mensagem padrão
          if [ -z "$COMMITS" ]; then
            COMMITS="Nenhum commit encontrado."
          fi

          # Envia a mensagem pro Discord
          curl -H "Content-Type: application/json" \
               -X POST \
               -d "{\"content\": \"📌 **Novos Commits Recebidos:**\n\n$COMMITS—————————————————————\"}" \
               $DISCORD_WEBHOOK_URL
